const assert=require('assert'),m=require('../js/health-integration');let a=0;const eq=(x,y)=>{a++;assert.strictEqual(x,y)},near=(x,y)=>{a++;assert.ok(Math.abs(x-y)<1e-9)};const now='2026-08-20',tx=(date,status='realizado',type='despesa',amount=100)=>({transaction_date:date,status,transaction_type:type,category:'Gastos Fixos',amount});const rows=[tx('2026-02-01'),tx('2026-03-01','pending'),tx('2026-04-01','realizado','investimento'),tx('2026-05-01','mystery'),tx('2026-06-01','cancelado'),tx('2026-07-01','realizado','despesa',100.5)];near(m.fixedAverage(rows,2026,8,now),200.5/6);eq(m.fixedForMonth(rows,2026,3,now),0);eq(m.fixedForMonth(rows,2026,4,now),0);const r=m.reserveSnapshot([{type:'aporte',amount:1000},{type:'retirada',amount:250}],{months:6},100);eq(r.balance,750);eq(r.target,600);eq(r.coverage,7.5);eq(m.reserveSnapshot([],{targetMode:'custom',customTarget:5000},0).target,5000);console.log(`reserve-canonical-v82: 1 test, ${a} assertions passed`);
